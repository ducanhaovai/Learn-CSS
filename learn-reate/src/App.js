import React from "react";
import { auth, database } from "./firebase";

const App = () => {
  // Sử dụng auth và database ở đây
  return (
    <div>
      <h1>My Firebase App</h1>
    </div>
  );
};

export default App;
